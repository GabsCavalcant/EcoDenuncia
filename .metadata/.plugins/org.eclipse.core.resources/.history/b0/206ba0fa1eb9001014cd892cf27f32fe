package br.com.ecodenuncia.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController // Diz ao Spring que esta classe vai responder requisições web
public class HealthCheckController {

    @GetMapping("/health") // Define a URL: http://localhost:8080/health
    public String healthCheck() {
        return "API EcoDenúncia está no ar! 🚀";
    }
}